name: Jira

on:
  pull_request:
    paths-ignore: ["*.md"]

jobs:
  call-workflow-jira:
    uses: workleap/wl-reusable-workflows/.github/workflows/reusable-jira-workflow.yml@main
    permissions:
      id-token: write
      contents: read
      pull-requests: write
